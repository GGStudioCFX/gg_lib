# Exported settings

Settings files saved from Script Studio land here, one per script:
`transfer/<script>.json`. **Import & Export** at the bottom of a script's page
writes the file and reads it back.
